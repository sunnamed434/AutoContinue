export const DEFAULT_CONFIG = {
  enabled: true,
  showNotifications: false,
  autoContinueCount: 0,
  timeSaved: 0,
  lastReset: Date.now(),
  idleTimeout: 5,
  autoClickDelay: 100,
  enableYouTubeMusic: true,
} as const;
